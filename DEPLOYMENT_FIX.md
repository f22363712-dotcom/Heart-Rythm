# 🚀 心动积分项目 - 云端部署指南

## 快速部署方案对比

| 平台 | 难度 | 成功率 | 推荐指数 |
|------|------|--------|----------|
| **Railway** ⭐ | ⭐ 简单 | 95% | ⭐⭐⭐⭐⭐ |
| Render | ⭐⭐ 中等 | 60% | ⭐⭐⭐ |
| Vercel | ⭐⭐⭐ 困难 | 50% | ⭐⭐ |

---

## 🎯 方案一：Railway 部署（推荐！最简单）

Railway 是最简单的部署平台，成功率最高！

### 步骤 1：注册 Railway

1. 访问 [https://railway.app](https://railway.app)
2. 点击 **Login** → 使用 **GitHub 账号**登录

### 步骤 2：创建新项目

1. 登录后，点击左侧的 **"New Project"** 按钮
2. 选择 **"Deploy from GitHub repo"**

### 步骤 3：选择仓库

1. 找到并选择 **Heart-Rythm** 仓库
2. 选择 **v2.1** 分支（重要！）
3. 点击 **"Import"**

### 步骤 4：配置服务（Railway 会自动检测）

Railway 会自动检测到 Python 项目，但需要确认配置：

| 配置项 | 值 |
|--------|-----|
| **Project** | Heart-Rythm |
| **Branch** | v2.1 |
| **Root Directory** | `./` (项目根目录) |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python server.py` |

### 步骤 5：部署

1. 点击 **"Deploy"** 按钮
2. 等待 2-3 分钟
3. 部署成功后，Railway 会提供一个 URL，例如：
   - `https://heart-rhythm.up.railway.app`

### 步骤 6：访问应用

- **API 文档**: `https://你的服务名.up.railway.app/docs`
- **API 根路径**: `https://你的服务名.up.railway.app/`

### ✅ Railway 的优势

- ✅ **自动检测配置** - 不需要手动设置 Root Directory
- ✅ **更快的部署速度** - 通常 1-2 分钟
- ✅ **更好的日志显示** - 调试更方便
- ✅ **自动 HTTPS** - 无需配置
- ✅ **免费额度充足** - 每月 $5 免费额度

---

## 🔧 方案二：修复 Render 部署（如果你坚持使用 Render）

### 问题根源

Render 的 Root Directory 被错误设置为 `src`，导致无法找到 `server.py`

### 解决方案 A：完全删除并重建服务

1. **删除现有服务**
   ```
   访问：https://dashboard.render.com
   找到：heart-rhythm-backend 服务
   操作：Settings → Delete Service（删除服务）
   ```

2. **等待清理完成**
   - 等待 1-2 分钟，确保 Render 完全删除服务

3. **使用直接链接创建新服务**
   ```
   访问：https://render.com/new/web-service
   ```

4. **填写配置（必须准确！）**

   | 配置项 | 值 | 注意事项 |
   |--------|-----|----------|
   | **GitHub Repository** | Heart-Rythm | 选择你的仓库 |
   | **Branch** | v2.1 | ⚠️ 必须选择 v2.1 |
   | **Name** | heart-rhythm-api | 自定义名称 |
   | **Environment** | Python 3 | 不要改 |
   | **Root Directory** | 【完全留空】 | ⚠️ 不要填写任何内容！ |
   | **Build Command** | `pip install -r requirements.txt` | 直接复制 |
   | **Start Command** | `python server.py` | 直接复制 |

5. **点击 "Create Web Service"**

### 解决方案 B：使用 Render CLI（高级）

如果控制台无法修复，可以使用命令行：

```bash
# 安装 Render CLI
npm install -g render

# 登录
render login

# 删除旧服务
render delete heart-rhythm-backend

# 创建新服务
render create python --name heart-rhythm-api
```

---

## 🎯 方案三：使用 Docker 部署（最稳定）

如果以上方案都不行，可以使用 Docker 容器部署。

### 创建 Dockerfile

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["python", "server.py"]
```

### 部署到 Render

1. 创建 `Dockerfile`（上面的内容）
2. 推送到 GitHub
3. 在 Render 创建服务时，选择 **Docker** 环境
4. Docker 会自动构建和部署

---

## 📊 部署检查清单

### 部署前检查

- [ ] 代码已推送到 GitHub 的 `v2.1` 分支
- [ ] `server.py` 存在于项目根目录
- [ ] `backend/api/main.py` 存在
- [ ] `requirements.txt` 包含所有依赖

### 部署后检查

- [ ] 服务成功启动（没有错误日志）
- [ ] 可以访问 `/docs` 路径
- [ ] API 返回正确的响应

### 常见错误排查

**错误 1**: `cannot open file 'server.py'`
- **原因**: Root Directory 配置错误
- **解决**: 确保 Root Directory 为空

**错误 2**: `ModuleNotFoundError: No module named 'backend'`
- **原因**: Python 路径问题
- **解决**: 检查 `server.py` 中的 `sys.path` 设置

**错误 3**: `Port already in use`
- **原因**: 端口冲突
- **解决**: 使用环境变量 `PORT`

---

## 🎉 推荐部署流程

```
1. Railway（最简单，推荐）
   ↓ 如果失败
2. Render（删除并重建服务）
   ↓ 如果失败
3. Docker 部署（最稳定）
```

---

## 💡 建议

**如果你是第一次部署**：强烈推荐使用 **Railway**
- 配置简单
- 自动检测项目结构
- 成功率高达 95%
- 部署速度快

**如果你需要更多控制**：可以使用 **Render**，但要确保：
- 完全删除旧服务
- Root Directory 留空
- 选择正确的分支（v2.1）

---

需要帮助？查看：
- [Railway 文档](https://docs.railway.app)
- [Render 文档](https://render.com/docs)
- [项目 Issues](https://github.com/f22363712-dotcom/Heart-Rythm/issues)
